import 'package:flutter/material.dart';

class IrregularitiesPage extends StatelessWidget {
  const IrregularitiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("sim"),
    );
  }
}
